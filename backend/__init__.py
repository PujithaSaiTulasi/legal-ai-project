# backend/__init__.py
# This empty file tells Python that "backend" is a package (a folder of modules).
# Without it, "from backend.config import ..." would fail with an ImportError.
# Every folder that contains Python modules you want to import needs one of these.
